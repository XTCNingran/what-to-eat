# 今天吃什么 — 版本记录

## v0.2.0（2026-04-21）

### 部署
- 上线至 Railway，公网地址：https://what-to-eat-production-11df.up.railway.app
- 单进程模式（`--workers 1`），保证房间状态一致性

### 新功能
- **主持人可参与答题**：创建房间时填写名字，自动加入为参与者；等待页新增"我也要答题"按钮
- **全员提交后自动出结果**：无需主持人手动点"看结果"，所有人答完即自动计算并跳转

### 题目优化
- 预算：`"60元以上随意"` → 拆成 `"60元以上"` 和 `"随便多少都行"`
- 菜系：`"快餐随便"` → 拆成 `"快餐"` 和 `"随便都行"`
- 距离：`"楼下就好"` → `"越近越好"`，`"都行不在乎"` → `"无所谓"`
- 热汤：`"不需要，随便"` → 拆成 `"不需要"` 和 `"随便"`
- 昨天吃什么：`"吃得还不错"` → `"正餐（坐下来吃的）"`

### Bug 修复
- 修复单人模式答题完成后卡在"已提交"页面的问题

---

## v0.1.0（2026-04-17 ~ 2026-04-21）

### 功能
- 多人房间：创建房间 → 扫码加入 → 集体答题 → 评分排名
- 单人模式：一键直达答题，无需等待他人
- 问卷系统：6道实用题 + 2道随机趣味题
- 评分引擎：按答题权重对178家长泰广场餐厅打分排名
- 黑名单：创建房间时可排除不想去的餐厅
- 结果自由选择：可点选任意名次，不限于第一名
- 历史记录：确认后写入，自动规避近两天去过的餐厅
- 餐厅管理页（`/manage`）：搜索、编辑、软删除、新增餐厅
- 局域网 QR 码：`python run.py` 启动后打印二维码

### 数据
- 餐厅数据：178家，来源长泰广场商户名单（2026-04-17）
- 数据格式：`data/restaurants.json`（含 name、rating、avg_spend、tags、vendor、deleted 字段）
