# Release Notes — v1.0.1（2026-05-19）

## 评分引擎
- 结果排名改为**加权随机抽样**：高分餐厅概率更高但不保证第一，每次结果有自然变化
- 趣味题权重 ×1.5，`random_bonus` 从 0.8 提升至 2.0，增加结果多样性
- 新增 `no_seat` 标签，标记无固定座位的餐厅（奶茶店、咖啡摊等），共标注 34 家
- "坐下来好好吃" 过滤逻辑改为排除 `no_seat` 餐厅，`fast_service` 语义收窄为"出餐快"，有座位的快餐（麦当劳、面馆等）不再被误排除
- 预生成 3 批结果（房间 ID + 批次索引作种子），所有客户端看到相同候选池

## 题目
- 预算选项 "60元以上" → "100元以上"
- 删除第五题（上一顿吃了什么）
- 所有趣味题移除"随便都行"选项
- 新增性格测试题（麻辣香锅 / 清蒸鱼 / 红烧肉 / 沙拉）

## 结果页
- 结果分组改为：餐厅（最多5家）+ 其他/饮料（最多2家）
- 移除饮料专属投票的二元过滤，改为顶部橙色提示（不影响其他人的推荐结果）
- 新增"换一批"按钮，最多切换3批，点到底弹出"别点了，不要吃了"
- 搜索框支持从数据库自动补全餐厅名，选择后记录历史
- 历史记录仅显示当前参与者的记录，不显示全局历史
- 单人模式按钮文案改为"就去：xxx"，多人模式保持"我们去：xxx"

## 其他
- 首页移除"管理餐厅"入口
- 首页底部新增版本号
- 加入房间码输入错误时显示提示文字
- README 新增推荐逻辑详细说明
